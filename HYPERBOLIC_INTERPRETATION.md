# 하이퍼볼릭 모델 해석 가이드 (곡률·수치 안정성·계층성)

실험 결과 해석과 튜닝 시 참고할 내용을 정리했습니다.

---

## 1. 곡률(Curvature) 설정

**역할:** 공간의 휨 정도. c가 너무 작으면 유클리드에 가깝고, 너무 크면 수치 불안정.

**현재 구현:**

- **기본값:** `--c 0.5` (train_hyperbolic_hypergraph.py). 보통 c=1.0을 쓰는 경우도 있으나, 이 데이터·구조에서는 0.5를 기본으로 둠.
- **Learnable Curvature:** `--learnable_curvature` **기본 True**. 곡률을 학습 가능 파라미터로 두어 고정되지 않음.
- **비활성화:** `--no_learnable_curvature` 로 고정 곡률 사용.

**튜닝:**

- 계층 깊이가 크다고 가정하면 c를 키워보기 (예: `--c 1.0`).
- NaN/폭주가 나오면 c를 줄이기 (예: `--c 0.2`, `0.4`).
- Learnable을 켜 두면 데이터에 맞게 곡률이 조정되므로, 우선 기본값으로 두고 학습·검증 곡선을 보는 것을 권장.

---

## 2. 수치적 불안정성 (Numerical Instability)

**위험:** exp, log, tanh 등으로 인해 임베딩이 Poincaré ball 경계에 가까우면 그래디언트 폭주/소멸.

**현재 구현:**

- **Clamping:** 임베딩·프록시를 ball 안쪽으로 유지 (예: `clamp(..., -0.99, 0.99)`, `-0.9, 0.9`).
- **Soft margin / Clamped loss:** 일부 loss에서 `clamp(..., min=-10, max=10)` 등으로 구간 제한.
- **Norm 처리:** `clamp(norm, min=eps)` 로 0 나눗셈 방지.
- **초기화:** 작은 gain(0.1 등)으로 초기값을 원점 근처로 두어 수치 안정성 확보.

**확인할 것:**

- 학습 로그에서 **Loss가 갑자기 NaN** 이 되는지.
- **특정 에폭 이후로 검증 지표가 거의 안 움직이는지** (vanishing/폭주 의심).
- 문제가 있으면: learning rate 낮추기, `--c` 낮추기, clamp 범위를 더 보수적으로(예: ±0.9).

Riemannian Adam 등 전용 옵티마이저는 현재 사용하지 않음; 필요 시 geoopt 등으로 확장 가능.

---

## 3. “계층 구조”가 데이터에 있는가?

**해석의 핵심:**  
조향사–레시피 관계가 **실제로 계층적(트리·파워로 분포)** 인지에 따라 하이퍼볼릭 이점이 달라짐.

- **유클리드가 유리한 경우:** 격자·평면에 가까운 관계, 계층이 약한 데이터.
- **하이퍼볼릭이 유리한 경우:** 트리·파워로 분포가 강한, 명확한 계층이 있는 데이터.

**실험 해석:**

- **베이스라인(예: HR@10 ~27%)이 하이퍼볼릭과 비슷하거나 더 좋다면**  
  → “이 데이터는 가정보다 계층적이지 않다”는 것이 실험으로 드러난 것으로 해석할 수 있음.  
  → 논문에서는 “데이터 특성상 유클리드로도 충분히 설명 가능”이라고 서술하는 것이 타당.
- **하이퍼볼릭이 일관되게 더 좋다면**  
  → 조향사–레시피 관계에 계층이 있다는 가정을 실험적으로 뒷받침하는 근거로 사용 가능.

**추가 분석 제안:**

- 조향사별 레시피 수 분포가 **파워로에 가까운지** (몇몇 조향사에만 레시피가 쏠리는지) 확인.
- 트리/계층 구조가 있다고 가정할 수 있는 메타데이터(스타일, 계보 등)가 있다면, 그에 따른 서브그룹 분석.

---

## 4. 계층 정보의 부재와 실험 결론

### 4.1 계층 정보의 부재

- **데이터:** 원본에는 **그룹–블렌더** 계층이 존재함 (블렌더 이름 ↔ 블렌더 그룹 매핑, 예: aldehydic, floral).
- **현재 모델:** 입력 레이어(Embedding Layer)는 **블렌더 인덱스만** 단일 차원으로 사용하며, **그룹 정보는 모델에 전달되지 않음** (Flat 구조).
- 따라서 모델 입장에서는 “계층”이 아닌 **인덱스 나열**만 보게 됨.

### 4.2 기하학적 왜곡

- 하이퍼볼릭 모델은 **내재된 계층 구조**를 전제로 최적화됨.
- **입력이 Flat(인덱스 나열)**일 때는, 오히려 유클리드 공간보다 **거리 계산의 왜곡**이 커질 수 있음.
- 즉, 계층을 반영하지 않은 Flat 입력에서는 하이퍼볼릭 기하의 이점이 살아나지 않고, 부작용만 커질 수 있음.

### 4.3 실험 결론 (정리)

- **현재의 Flat한 데이터 입력 환경**에서는 **유클리드 기반 CML(예: HR@10 ~27.3%)이 최적의 성능**을 보이는 것으로 확인됨.
- **하이퍼볼릭 모델의 이점을 살리려면**,  
  **그룹 정보를 반영한 Hierarchical Multi-task Learning** 등 **구조 개선이 선행**되어야 함.
- 요약: “데이터에는 그룹–블렌더 계층이 있으나, 모델이 이를 사용하지 않아 Flat 입력이 되었고, 그 결과 유클리드 CML이 우수하며, 하이퍼볼릭 이점을 내려면 계층(그룹) 반영 구조가 필요하다”는 결론으로 정리할 수 있음.

---

## 요약

| 항목 | 현재 구현 | 권장 |
|------|-----------|------|
| **곡률 c** | 기본 0.5, Learnable 기본 True | 계층 깊이·안정성 보고 튜닝; Learnable 유지 권장 |
| **수치 안정성** | Clamp, soft margin, 작은 초기화 | NaN/지표 정체 시 lr·c·clamp 점검 |
| **계층성 해석** | — | 베이스라인 vs 하이퍼볼릭 비교로 “데이터의 계층성” 논의 |
| **계층 정보** | 입력에 그룹 미반영 (블렌더 인덱스만) | 그룹 반영 Hierarchical Multi-task 등 구조 개선 후 하이퍼볼릭 재평가 |
